let option = {
    merchantId, accountPool, marketplaceId, clientId
}
function initAuth(domain, pageId, assochandle, maxAuthAge, redirectUri ,cancelURL, siteSate, option, ssoApp, callback){
    return new Promise()
}
